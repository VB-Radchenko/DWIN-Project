
V7.26
增加辅助变量控件
优化ASCII输入预览

V7.27
修正任务栏在左侧或者右侧时显示不正常的问题

V7.36
优化辅助变量_数据传递 控件
优化ASCII输入预览
增加版本说明

V7.37
增加HEX数据显示
增加文本滚屏显示

V7.38
修改变量图标显示中上限自动添加的bug
增加DGUS II文本显示

V7.381
修改DGUS II文本显示：Y方向点阵数为文字缩放比例

V7.382
修改DGUS II文本显示：修正字符间距自动调整功能显示问题

V7.383
动画图标功能升级，增加动画显示模式：循环播放模式与单次播放模式
其中：循环播放模式为原先模式
单次播放模式，变量为停止值时，从结束图标到开始图标，逆序播放一次；变量为开始值时，顺序播放一次；变量为其他值时，显示停止图标

V7.50
此版本针对T5L
主要增加功能：修改字库限制，可以任意选择，不局限于0或者大于等于23


V7.51
此版本只适用于T5L
主要增加功能：修改ICO生成工具为ICL，同时软件支持ICL文件

V7.52
此版本只适用于T5L
主要增加功能：
1、升级ICL生成工具；
2、增加图标透明显示时,背景色滤除的强度设置0-63，0为默认值：0x10

V7.53
此版本只适用于T5L
主要增加功能：
1、修改内部问题；
2、升级icl生成工具

V7.54
此版本只适用于T5L
主要增加功能：
1、修正预览功能；
2、升级icl生成工具；
3、修正新建功能。

V7.55
此版本只适用于T5L
主要增加功能：
1、增加功能：JPEG图标页面平移显示，其中：VP指针，占用4个字，
     VP，当前显示窗口在图标页面上的显示其实位置，（x，y）2个字；
     VP+2：移动距离
     VP+3 ：保留
     移动模式：ICON对应移动方向的坐标可以很大，只要不超过JPEG文件大小限制即可
     文件大小限制标准：T5L1为252kb，T5L2为764kb
2、增加功能：滑动图标选择，此功能配合PEG图标页面平移显示功能使用，对应的VP应与此功能相同
      其中：移动页面ID，可以在左侧建立一个模拟页面，在页面上添加触控，然后在此添上对应的页面ID，JPEG移动后，可以响应对应的触控。
                       
V7.56
此版本只适用于T5L
主要增加功能：
1、增加功能：变量数据 JPEG 图标叠加，其中：VP指针，占用4个字，
     VP：写入 0x5AA5 表示 JPEG 数据有效，显示开启；
	VP+1：JPEG 数据缓冲区字（Words）长度，必须是偶数；
	VP+2：JPEG 数据开始。
2、增加功能：WAE生成工具
      用于WAE生成，主要是用于wav文件存储        
3、增加批量数据图标快速复制粘贴功能
其中：        
 数据地址指针，必须为偶数。
1 个数据项目对应两个地址，一共 Data_Num 个数据项：
	D3：0x5A 表示数据有效（显示图标），其余表示无效（不显示图标）。
	D2：需要显示的图标编号，0-N。
	D1：图标显示模式，仅对图标背景图片复制显示有效。
	.7 背景滤除控制 0=滤除（背景不显示） 1=背景显示。
	.6 保留，写 0.
	.5-.0 背景滤除强度 0x01-0x3F。
	D0：图标显示亮度（0x00-0xFF），仅对图标背景图片复制显示有效。      
数据对应图标显示位置描述指针，必须为偶数，每个图标 2 个地址：
	D3:D0：图标显示位置左上角坐标（x，y）；
	数据按照数据源编号（0-（Data_Num-1））顺序描述。
需复制的每个图标数据描述指针，必须为偶数，每个图标 4 个地址：
	D7:D4：图标左上角坐标（x，y）；
	D3:D2：图标宽度像素值；
	D1:D0：图标高度像素值；
	数据按照图标编号（0-N）顺序描述。
Data_Num，对应的数据项目数量，0x0000-0x400
图标来源：
	0x00：当前显示页面（显存模式）。
	其余：图标背景图片（Flash 读取）
	
V7.57
此版本只适用于T5L
主要增加功能：
1、增加16级灰度字库生成软件。
2、增加下载功能， 可以通过串口直接下载DWIN_SET里面的所有的工程文件。

V7.58
此版本只适用于T5L
主要增加功能：
1、修正滑动图标选择，把滑动图标对应的图片命名为1000-1023的页面，然后加入，可以增加触控控件，同时在控件属性中页面选择对应命名的页面即可。
2、修正部分内部问题。

V7.59
此版本只适用于T5L
主要增加功能：
1、增加网络通讯配置，针对需要云平台开发的项目，可以把相关的配置在22文件中直接生成；
2、优化灰度字库的生成部分；
3、优化各项配置工具；
4、修正部分内部问题。

V7.591
此版本只适用于T5L
主要增加功能：
手势翻页增加结束动画，并且动画速度可控。
0x00：结束无动画，兼容以前模式。
其余：定义为结束动画速度，单位为 像素点/DGUS周期，推荐值为 水平分辨率/20 。

V7.592
此版本只适用于T5L
主要增加功能：
变量数据显示时，增加无效零显示/不显示的选择；
修复部分内部问题。

V7.593
此版本只适用于T5L
主要增加功能：
增加文本显示居中功能；
修复部分内部问题。

V7.594
此版本只适用于T5L
主要增加功能：
1、HEX显示，增加了把HEX转换成BCD码显示的功能，添加数据 BCD 码调整使能选择，BCD 码调整使能开启时，0x0A 将调整为 0x10 显示为 10
2、增加T5L51.bin文件下载功能；
3、修改icl生成工具

V7.595
此版本只适用于T5L
主要增加功能：
1、增加二维码固定大小显示
2、优化22文件网络配置相关的功能。

V7.596
此版本只适用于T5L
主要增加功能：
1、修改按键值返回功能，增加了按压时间门槛设置的功能，设置时间单位为0.1s

V7.597
此版本只适用于T5L
主要增加功能：
1、增加组态图标字库滚字轮显示
其中：
数据指针，每个数据占 4 个字存储空间，定义如下：
VP=被选中数据存储地址。
VP+1=调节参数，对应手势调节的 VP+1 或增量调节的 VP。
VP+2=系统保留，显示偏移量，整数。
VP+3=系统保留。
滚动速度（每个 DGUS 周期滚动的像素点），0x01-0xFF

V7.598
此版本只适用于T5L
主要增加功能：
1、组态图标字库滚字轮显示增加数据变量间隔步长功能；
2、修改区域滚屏显示的平移数据保存地址

V7.600
此版本只适用于T5L
主要增加功能：
1、图标动画增加动画播放功能；
2、增加控件锁定功能，并可以批量锁定解锁，便于编辑好后，位置不能鼠标拖动；
3、修复一些已知BUG

V7.610
此版本只适用于T5L
主要增加功能：
1、优化icl生成工具；
2、针对浮点数据以及BIG5等数据初始化显示优化；
3、修复一些已知BUG

V7.611
此版本只适用于T5L
主要增加功能：
1、触控干涉检测，变为操作者决定是否继续生成。

V7.612
此版本只适用于T5L
主要增加功能：
1、动画图标显示增加停止时状态。

V7.613
此版本只适用于T5L
主要增加功能：
1、优化icl生成工具。

V7.614
此版本只适用于T5L
主要增加功能：
1、修正批量锁定功能。

V7.615
此版本只适用于T5L
主要增加功能：
1、修正初始值设置造成生成工程报错的问题。

V7.616
此版本只适用于T5L
主要增加功能：
1、修正图片加密时的问题。

V7.617
此版本只适用于T5L
主要增加功能：
1、文本滚屏显示增加一直滚动，不管文本长度。

V7.618
此版本只适用于T5L
主要增加功能：
1、优化文本显示预览功能；
2、修正一些已知bug

V7.620
此版本只适用于T5L
主要增加功能：
1、滑动手势翻页增加滑动方向；
2、修正一些已知bug

V7.621
此版本只适用于T5L
主要增加功能：
1、优化icl工具；
2、修正一些已知bug

V7.621
此版本只适用于T5L
主要增加功能：
1、修复预览显示异常

V7.624
此版本只适用于T5L
主要增加功能：
1、增加GTF 图标字库文本显示